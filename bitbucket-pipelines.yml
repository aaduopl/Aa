pipelines:
  default:
    - step:
        name: Run Python Script
        image: python:3.10-alpine
        script:
          - echo "Starting pipeline..."
          - apk update && apk install -y python3 python3-pip
          - pip install telebot pymongo aiohttp python-telegram-bot && chmod 755 megoxer && python3 m.py
